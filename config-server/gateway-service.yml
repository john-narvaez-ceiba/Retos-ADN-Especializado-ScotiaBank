server:
  port: 8080
eureka:
  client:
    serviceUrl:
      defaultZone: http://root:scotiabank@localhost:8099/eureka/
zuul:
  routes:
    book-service:
      path: /book-service/**
    eureka-service:
      path: /eureka-service/**