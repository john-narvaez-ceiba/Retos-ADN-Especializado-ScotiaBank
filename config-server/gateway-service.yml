server:
  port: 8080
eureka:
  client:
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
zuul:
  routes:
    book-service:
      path: /book-service/**
      serviceId: GREETING-SERVICE
      stripPrefix: false